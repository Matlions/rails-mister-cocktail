<div class="container">
  <%= simple_form_for [@cocktail, @dose] do |f| %>
  <%= f.association :cocktail %>
  <%= f.association :ingredient, as: :select %>
  <%= f.input :description %>
  <%= f.button :submit %>
  <% end %>
</div>

