<div class="container">
  <h2>
  <strong>Name:</strong>
  <%= @cocktail.name %>
</h2>
  <div class="card-ingredient">
    <strong><p>Ingredients</p></strong>
  <% @cocktail.doses.each do |dose| %>

   <p> <%= dose.description %> de <%= dose.ingredient.name %> </p>

    <button type="button" class="btn btn-success"> <%= link_to '  delete', dose_path(dose), method: :delete %></button>

  <% end %>
  </div>

  <button type="button" class="btn btn-warning"><%= link_to 'Add Ingredients', new_cocktail_dose_path(@cocktail),class: "btn-add" %></button>
  <button type="button" class="btn btn-light"><%= link_to 'Home Page', cocktails_path, class: "btn-add" %></button>



</div>




